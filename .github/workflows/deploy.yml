name: Publish Docs using uv
on:
  push:
    branches:
      - main

permissions:
  contents: write

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Install uv
        uses: astral-sh/setup-uv@v5
        with:
          enable-cache: true

      - name: Install dependencies
        run: uv sync --all-extras --dev
        working-directory: ./backend   # 👈 이 줄을 추가해서 "backend 폴더에서 실행해"라고 알려줍니다.


      - name: Configure Git Authorship
        run: |
          git config --global user.name "GitHub Actions"
          git config --global user.email "actions@github.com"

      - name: Deploy docs
  # --project backend 옵션을 추가합니다.
  # 해석: "현재 위치(Root)에서 실행하되, 파이썬 환경 설정은 backend 폴더를 참고해라"
        run: uv run --project backend mkdocs gh-deploy --force

